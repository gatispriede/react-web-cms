# Kamal destination overlay — skyclimber.pro.
#
# Loaded on top of `config/deploy.yml` when CI invokes
# `kamal app boot --destination=skyclimber --version=<sha>`. Carries
# only the values that differ from the shared base.

servers:
  web:
    hosts:
      - skyclimber.pro
