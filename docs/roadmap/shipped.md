# Shipped roadmap items

Archive of completed roadmap entries. Latest first. Active backlog lives in [README.md](README.md).

## 2026-05-14 — C3 bulk image upload with ratio (re-confirmed complete)

| # | Notes |
|---|---|
| **C3 bulk image upload with ratio** | Re-confirmed [content/bulk-image-upload-with-ratio.md](content/bulk-image-upload-with-ratio.md) is fully shipped — the original C3 jump (2026-04-24) plus the C2 follow-up (2026-05-14) together cover every acceptance criterion, so this jump is a verification + docs pass with no new code. **What already exists:** (1) **Server** — `POST /api/upload-batch` (`ui/client/pages/api/upload-batch.ts`) accepts `multiple` `file[]` parts + a `urls` field + a `ratio` field (`free / 1:1 / 4:3 / 3:2 / 16:9`); every file *and* URL-fetch path runs `optimizeImageFile({ratio})` — sharp orient → cover-crop to ratio → resize cap → recompress → EXIF strip — then `buildImageRecord` persists the full optimisation/provenance shape; collisions get a `-N` suffix; per-file `PerFileResult[]` means one bad file never aborts the batch. (2) **Admin UI** — `BulkImageUploadModal` (`ui/admin/lib/BulkImageUploadModal.tsx`): drop-zone + file-picker + `From device`/`From URL` tabs + aspect-ratio `Select` (pre-filled from the caller) + XHR upload with real progress + per-file error `List`. (3) **Gallery integration** — `GalleryEditor` (`ui/admin/modules/Gallery/GalleryEditor.tsx`) has a "Bulk upload" button next to its aspect-ratio select, pre-fills the modal with the gallery's current ratio, and `handleBulkUploaded` appends each accepted image as a new gallery item; the editor's ratio select + bulk-upload button carry `data-testid` (`gallery-editor-aspect-ratio-select` / `gallery-editor-bulk-upload-button`). (4) **MCP parity** — `image.upload` (`services/features/Mcp/tools/images.ts`) accepts the same `ratio` enum and runs the identical sharp cover-crop, so the AI write path matches the human bulk-upload flow. **Deferred:** per-image crop-handle UI (drag centre pre-upload — the `position:'attention'` heuristic covers most phone shots); `data-testid` on `BulkImageUploadModal.tsx`'s *own* controls — that file lives under `ui/admin/lib/*`, owned by a concurrent agent track, so the modal's testid pass is left to whichever jump next touches it (a noted agent-overlap, not a gap in this feature). Typecheck (`ui/client`) clean on all relevant files. Spec doc Status block + Acceptance ✅ marks amended; `content/README.md` C3 row struck. |

## 2026-05-14 — W2.5b admin empty states + onboarding tie-in (operator-grade EmptyState)

| # | Notes |
|---|---|
| **EMP empty states + onboarding** | Upgraded the shared admin `EmptyState` to operator-grade and wired it across every high-traffic admin pane. **What already existed (prior pass):** `ui/admin/lib/EmptyState.tsx` rendering an AntD `<Empty>` with title/description/primary/secondary slots, plus all 14 panes already passing `emptyState` copy + `data-testid`s via the module-composed bridges (`AdminCrudListModule`'s `emptyState` prop) or direct `<EmptyState>` usage, and the full i18n key set (`empty.*` in `en.json` / `lv.json`). **What this jump added:** (1) **Illustration set wired in** — `ui/admin/lib/emptyStateArt/index.tsx` (a 15-key inline-SVG set keyed per surface — `pages`/`posts`/`products`/`themes`/`users`/`orders`/`inventory`/`customers`/`inquiries`/`trash`/`audit`/`errors`/`mcp`/`languages`/`generic`, each a 240×180 `currentColor`-stroked frame that tracks light/dark mode) was untracked + unconsumed; `EmptyState` now takes an `art?: EmptyStateArtKey` prop resolved via `emptyStateArt()`, with `icon` kept as a one-off override. Replaced the bare AntD `<Empty>` glyph with the keyed illustration; added an `{testId}-illustration` testid. (2) **`onboardingCta(label, testId)` helper** — exported from `EmptyState.tsx`, builds a ready-made `secondary` `EmptyStateAction` that deep-links via `window.location.assign('/admin/onboarding?force=1')` into the first-run wizard (the standalone route, not a registry pane — so `force=1` re-runs guided setup without wiping content). This is the onboarding tie-in mechanism. (3) **`AdminCrudListModule` extended** — `AdminCrudListEmptyState` gained `art` + `secondary`, both passed through to `EmptyState`. (4) **Panes wired** — `art` keys added to all 16 EmptyState surfaces (Posts / Products / Users / Orders / Inquiries / Trash / Mcp tokens / Redirects / Permissions / Publishing / Inventory runs / Themes / Audit / Error log / Notifications / SEO overview / Backups); `onboardingCta` secondary added to the three content-creation panes (Posts / Products / Users) so a stuck operator on an empty pane can always route back to guided setup. (5) i18n — `empty.cta.guidedSetup` added to `en.json` + `lv.json`; `empty.posts.secondary` repurposed from the never-wired "Import sample bundle" to the guided-setup label. **Deferred:** the 14 hand-drawn Stitch illustrations (explicitly out of AI budget per the spec — the inline-SVG set is the operator-grade stand-in); the Languages + Inventory-products panes don't currently render an `EmptyState` at all (their `empty.languages.*` / `empty.inventoryProducts.*` keys pre-exist unused — out of scope for this high-traffic-CRUD pass); the `/admin/setup?force=1` SSR re-trigger gate (wizard route is `/admin/onboarding`, gated by `isFreshInstall()` server-side — the `?force=1` deep-link is wired client-side but the SSR gate relaxation is a separate change). Typecheck (`ui/client`) + eslint clean on all touched files; the `clientItemTypes.ts` Product*/Breadcrumb errors are pre-existing + out of scope. Spec doc [admin/admin-empty-states-onboarding.md](admin/admin-empty-states-onboarding.md) marked shipped; README.md EMP row + Wave 2.5b entry struck. |

## 2026-05-14 — Image optimisation on upload (completed — gap-close on the 2026-04-24 partial)

| # | Notes |
|---|---|
| **C2 image optimisation on upload** | Closed the remaining gaps from the 2026-04-24 partial ship of [content/image-optimization-on-upload.md](content/image-optimization-on-upload.md). The shared pipeline (`services/features/Assets/imageOptimize.ts` — orient → optional cover-crop → resize 1920 cap → recompress jpeg/png/webp/avif → strip EXIF → size guard) and `buildImageRecord` (`services/infra/imageMetadata.ts` — the persisted-record shape with `width`/`height`/`sizeBytes`/`originalName`/`uploadedBy`/`uploadedAt`/`optimised`/`format`) already existed and `/api/upload` already used both. **What this jump added:** (1) **`/api/upload-batch` now builds the record via `buildImageRecord`** for both the file path and the URL-fetch path — it previously wrote a bare `InImage` literal and silently dropped every optimisation/provenance field, so the two endpoints persisted *different* shapes despite the spec's "one pipeline, one record" intent. (2) **`OptimizeResult.readable` flag** — distinguishes corrupt/undecodable bytes from valid-but-not-transcoded formats (SVG/GIF). All three write paths (`/api/upload`, `/api/upload-batch` file + URL, `image.upload` MCP tool) now **reject corrupt input with 400 / `{ok:false}`** *before* writing, so a half-written/garbage file never lands in `public/images/` (spec "Failure modes"); SVG/GIF still pass through untouched. (3) **`readImageMetadata(srcPath)` helper + `AssetService.rescanDiskImages`** now extracts `width`/`height`/`format` for raster files it recovers — rescanned records carry the same dimension metadata a fresh upload would (spec "still persist dimensions for raster paths"); it reads metadata only, no wasteful re-encode of already-optimised on-disk files. (4) **`image.upload` MCP tool runs the optimise pipeline** — it used to write raw base64 bytes verbatim; MCP is the canonical AI write path so it now gets the same resize/recompress/EXIF-strip as the admin form, persists the full record shape, and returns `width`/`height`/`optimised`. (5) Test count 6 → 9 on `imageOptimize.ts` — added `readable` assertions + `readImageMetadata` coverage; still no checked-in binaries (sharp composes fixtures in-memory). **Confined to** `imageOptimize.ts` / `imageMetadata.ts` / `AssetService.ts` / `Mcp/tools/images.ts` / `upload.ts` / `upload-batch.ts` / the test + the 3 roadmap docs — no `ui/admin/*` or `ui/client/modules/*` touched. **Still deferred (unchanged):** the GraphQL SDL exposure of the metadata fields — `IImage`/`InImage` are GraphQL-generated; the fields are now persisted to Mongo on *every* ingest path (write-through, schemaless), so the SDL migration is a pure read-side change with no back-fill. `sharp` was already in `dependencies`. Typecheck (`services` + `ui/client`) + eslint clean on all touched files; the 2 `ui/admin/lib/emptyStateArt/index.tsx` TS2783 errors are pre-existing + out of scope. Spec doc amended (Status block + as-shipped Files + MCP coverage note + Acceptance ✅ marks); README.md C2 row updated. |

## 2026-05-14 — Wave 0d testid-coverage CI (AST-walk script + PR hard gate)

| # | Notes |
|---|---|
| **testid-coverage CI** | The `tools/scripts/testid-coverage.mjs` CI script from [platform/testid-coverage-ci.md](platform/testid-coverage-ci.md), closing the last universal-requirement gate (#4 — `data-testid` on every interactive surface) that had no automation. **Script:** AST-walks `.tsx` files via `@typescript-eslint/parser` (no regex parser) and visits every `JSXOpeningElement`. Flags as interactive: HTML primitives (`button` / `a` / `input` / `select` / `textarea` / `details` / `summary`), AntD interactives (`Button` / `Input` / `Select` / `Modal` / `Drawer` / `Tabs` / `Menu` / `Switch` / `Radio` / `Checkbox` / `DatePicker` / `TimePicker` / `Upload` / `Form.Item`, plus any sub-component on those namespaces e.g. `Radio.Group` / `Input.TextArea`), any element with an `onXxx` handler prop, and any element with an interactive `role=` (`button` / `tab` / `link` / `checkbox` / `radio` / `switch` / `menuitem` / `option` / `combobox`). Elements lacking `data-testid` (literal **or** dynamic `{...}` form — dynamic counts as present) are violations; testid values that aren't lowercase kebab-case `feature-component-role` emit **warn-only** naming warnings. Static `<div>`/`<span>`/`<p>` with no handler + no role are not flagged. **Modes:** default diff-mode (`git diff --diff-filter=AM origin/master...HEAD`, falls back to `HEAD` diff on shallow clones), `--all` for the full `ui/**/*.tsx` walk, `--quiet` to suppress naming warnings. **Allowlist:** `tools/scripts/.testid-coverage-allow` — glob path-patterns (`*` segment / `**` any-depth / `?` char), excludes `**/*.test.tsx`, `**/*.stories.tsx`, `**/_archived/**`, `**/__fixtures__/**`, `ui/client/modules/*/i18n/**`, `ui/admin/features/Debug/**`. Exit codes: 0 clean, 1 missing-testid violations (hard), 2 internal error. Output mirrors `mcp-schema-drift.mjs` — ✓/✗ symbols, `file:line`, summary counts, warnings on stderr non-blocking. **Test:** `tools/scripts/testid-coverage.test.mjs` — 10 `node:test` cases (Node's built-in runner, no vitest drag-in) over bad/good/naming fixtures; the bad fixture carries 6 known violations across 6 tag kinds, the good fixture 6 known-good interactive elements + 2 static; covers line-number capture, dynamic-testid acceptance, role-only interactivity, member-expression JSX (`Form.Item`), custom-component `onSubmit`. **CI:** `data-testid coverage (changed files)` step in `.github/workflows/ci.yml` after `Tests`, **PR-only** (`if: github.event_name == 'pull_request'`), `git fetch --depth=1 origin master` then `npm run testid-coverage` — **hard gate**, a missing `data-testid` in any changed `.tsx` fails the build. Diff-mode only by design — no big-bang sweep of the legacy backlog (~856 full-tree / ~526 diff-mode violations on the current tree; only new/modified files are gated, the rest is opportunistic cleanup). **`package.json`:** `testid-coverage` + `testid-coverage:test` scripts; `@typescript-eslint/parser` was already a devDep. **Deferred:** the optional `data-edit-target` second gate — lands with [admin-inline-editing.md](admin/admin-inline-editing.md), which introduces that attribute. Spec doc marked shipped; README.md universal-requirements line struck + Wave 0d entry struck. |

## 2026-05-14 — W0c Sonner toast system (dark-mode-aware Toaster)

| # | Notes |
|---|---|
| **SON admin toast system** | The Sonner adoption from [admin/admin-toast-system-sonner.md](admin/admin-toast-system-sonner.md). Most of the migration had already landed incrementally: `sonner@^1.7.4` is in `package.json` + `node_modules`, `ui/admin/lib/notify.ts` already wraps `sonner`'s `toast.*` (exporting `notifySuccess` / `notifyError` / `notifyWarning` / `notifyInfo` / `notifyPromise` / `notifyDestructive`), the dozens of `ui/admin/features/**` ViewModels already call those helpers, and a bare `<Toaster richColors closeButton position="bottom-right" duration={4000} />` was mounted once in `ui/admin/shell/AdminApp.tsx`. **The gap this jump closed:** the mounted Toaster had no dark-mode wiring and no `data-testid`. New `ui/admin/lib/AdminToaster.tsx` — a function-component wrapper that subscribes to the `@admin/lib/adminDarkMode` store via `useAdminDarkMode()` and feeds Sonner's `theme` prop (`'dark'` / `'light'`) so toasts flip in lockstep with the top-bar `DarkModeSwitcher` + the AntD `ConfigProvider` algorithm. `AdminApp` is a class component so it can't `useAdminDarkMode()` directly — the wrapper is the function component that can. `AdminApp.tsx` swapped its inline `<Toaster/>` for `<AdminToaster/>` (one-line import + one JSX node, per the "be minimal in shell files" constraint). **`data-testid`:** Sonner v1.x's `ToastOptions` doesn't accept `data-testid`, so the e2e hook is a wrapping `<div data-testid="admin-toaster">` for "the toast surface exists" plus a stable `admin-toast` class on every rendered toast (`toastOptions={{className: 'admin-toast'}}` → `.admin-toast[data-sonner-toast]`) for per-toast text assertions. The public `notify.ts` API was left **byte-identical** — zero call-site changes. Typecheck (`ui/client/tsconfig.json`) + eslint clean on all touched files. **Note for follow-up:** the spec's broader acceptance items (zero `message.*` AntD calls remaining, `toast.promise` on every ServiceLoader mutation, Undo on every destructive mutation, the `no-restricted-imports` ESLint rule banning `import {message} from 'antd'`, e2e smoke assertions) were **out of scope for this jump** — they touch `ui/admin/features/**` which other agents own; `notify.ts` already exposes `notifyPromise` + `notifyDestructive` so those are mechanical per-pane follow-ups. Spec doc amended with a Status section. |

## 2026-05-14 — W6b faceted filter system (filter-state lib + chip UI + `/products` wiring)

| # | Notes |
|---|---|
| **FFS faceted filter system** | The reusable faceted-filter system from [storefront/storefront-faceted-filter-system.md](storefront/storefront-faceted-filter-system.md), shipped as one chunk: the filter-state lib + the chip/facet-control UI + `/products` wired as the fully-working reference consumer. **Filter-state lib (`ui/client/lib/facetedFilter/`):** `types.ts` (IFacetConfig / FilterState / IFilterChip / ISavedSearch — facet *kinds* are `multi-select` / `single-select` / `range` / `boolean`), `filterUrl.ts` (URL ↔ state codec — the pre-existing core), `chips.ts` (derive / remove pinned chips — pre-existing core), and the net-new pieces: `useFilterState.ts` (Next-router URL binding hook — `router.replace({shallow:true})` so refresh / back-button / copy-paste reproduce the result set; honours facet `dependsOn` cascading — changing a parent facet clears dependants), `savedSearches.ts` (localStorage-backed saved-search store, in-memory fallback for privacy modes — the customer-facing side of "save this search"), `applyFilters.ts` (`applyFilterState` / `computeFacetCounts` — client-side `FilterState`→predicate matching + per-option live counts computed against all *other* facets, for in-memory ≤200-row storefront feeds; server-side `$facet` aggregation can swap in later without touching the URL scheme or components), `productListConfig.ts` (`PRODUCTS_LIST_CONFIG` + `CARS_LIST_CONFIG` — per-route facet schemas; these are **code-defined**, not operator-authored content, so no admin UI / MCP surface — the one editable surface, the customer's saved search, is the `savedSearches` store), `index.ts` barrel. **Chip / facet-control UI (`ui/client/components/FacetedFilter/`):** `FilterChipBar` (pinned removable applied-filter chips + "Clear all"), `MultiSelectFacet` / `SingleSelectFacet` / `RangeFacet` / `BooleanFacet` (one control per facet kind — multi/single render live counts, zero-count options stay visible but disabled per Baymard; RangeFacet uses paired min/max text inputs that commit on blur/Enter, remounted via `key` on URL change to avoid setState-in-effect), `FacetSidebar` (dispatches controls by `facet.kind` in declared `order`), `FacetedFilterPanel` (the wired drop-in — chip bar + sidebar + `SaveSearchPrompt`; the save-search prompt only surfaces for logged-in customers via `useSession`, and on save persists the canonical query string through the `savedSearches` store), `FacetedFilter.scss` (theme-token-driven, hoisted into `_app.tsx`'s global-SCSS import chain), `index.ts` barrel. `data-testid` on every chip / checkbox / radio / range input / clear button / facet group. **`/products` wired** (`pages/products/index.tsx`) — replaced the bare AntD category `<Select>` with a two-column layout: `FacetedFilterPanel` (price range + dynamic category multi-select + in-stock boolean) beside the result grid; the page parses the URL into `FilterState`, derives live category options + per-option counts from the product set, and filters the grid client-side. **Saved-search integration:** the `SaveSearchPrompt` component + `SavedSearchList` module already existed; this chunk supplies the `savedSearches` store they read/write — localStorage today, with the module comment + spec noting the server-backed `ISavedSearch` collection + alert worker + canonical `savedSearch.ts` MCP write path land with the customer-account roadmap item (saved searches require customer auth — a stated spec dependency). **Deferred** (noted inline in `pages/cars/index.tsx`): `/cars` URL-state sync — the cars filter state lives inside `CarsListHost` (`modules/_CarsPageModules/wrappers.tsx`) as component state, and that module dir is owned by the Cars-batch composition; swapping its `useState` for `useFilterState(CARS_LIST_CONFIG.facets)` is a clean one-file follow-up in that module, deferred to avoid a cross-agent edit. Also deferred to follow-up items: server-side `$facet` aggregation for large catalogues, the saved-search server backend + alert worker + MCP tool, and the mobile filter drawer. Typecheck + eslint clean on all touched files (pre-existing `clientItemTypes.ts` / `pages/api/account/*` errors untouched). Spec doc + README.md (FFS row + W6b Wave 6 entry struck) amended. |

## 2026-05-14 — Radical per-module theme variants (first pass: Gallery / Hero / Timeline)

| # | Notes |
|---|---|
| **Radical per-module theme variants** | First pass of the companion-to-first-class-themes jump. The 3 named-first modules — **Gallery, Hero, Timeline** — now carry genuinely *structural* per-theme variants (layout / arrangement / animation), not just colour + font, across all 5 first-class themes. **Pure CSS, zero DOM change** — no module `.tsx` was touched. Variants live in each theme's `services/themes/<slug>/module-styles.scss`, scoped under the established `[data-theme-name="<slug>"]` body-attribute convention (set in `pages/_document.tsx`); these partials are already `@use`'d into `ui/client/styles/globals/global.scss`. **Important correctness fix:** the pre-existing Hero/Gallery/Timeline blocks in those files were written against an *imagined* DOM (`.hero__inner`, `.gallery`, `.gallery__item`) that the modules never emit — they were inert. Rewrote every block against the real DOM contract: Gallery = `.gallery-wrapper.gallery-wrapper-app > .gallery-wrapper-images > .container > (.image, .text)`; Hero = `.hero > .hero__main` (+ `.hero__portrait` / `.hero__meta` / `.hero__coords`, `.hero--has-portrait` / `.is-fullbleed` modifiers); Timeline = `.timeline > .timeline__entry > (.timeline__when, .timeline__body)`. **Variant matrix:** **Gallery** — editorial = broken/asymmetric grid (dense auto-flow, span rhythm), agency = full-bleed horizontal scroll-snap banner band, commerce = tight uniform 4-up catalogue grid, saas-landing = overlapping depth-stack on a tilted plane, restaurant = single-column alternating photo+caption list. **Hero** — editorial = photo-bleed bottom-left, agency = centred overlay (portrait pulled behind copy), commerce = tight 50/50 split, saas-landing = split with floating glow-shadowed portrait, restaurant = photo-bleed bottom-centre. **Timeline** — editorial = broken-grid alternation across a centred hairline, agency = horizontal scroll-snap track on a connector rule, commerce = compact dense vertical spine, saas-landing = horizontal track with gradient connector + glowing nodes, restaurant = formal centred spine with symmetric L/R alternation. All animation/motion uses the `--motion-duration-*` / `--motion-ease-*` / `--motion-distance-*` / `--motion-scalar` token system (no hardcoded ms/cubic-bezier); all colour/radius via `--color-*` / `--theme-borderRadius`. Each block carries a comment naming its structural axis vs sibling themes + mobile fallbacks (grids collapse, horizontal tracks narrow, broken/centred spines fall back to a single left rail). `npx stylelint services/themes/**/module-styles.scss` clean; `global.scss` compiles. **Remaining** (next pass, ~S-M): **ProjectGrid**, **OrderProgressTimeline**, **Footer** still get only token-reskin per theme — no structural variant yet. ROADMAP.md entry + `storefront/first-class-themes.md` follow-up note amended. |

## 2026-05-14 — W2.5a admin command palette (kbar ⌘K)

| # | Notes |
|---|---|
| **CMD command palette** | kbar-style ⌘K / Ctrl+K command palette for the admin shell, completing the partial scaffolding under `ui/admin/shell/CommandPalette/`. **What already existed (scaffolded, unwired):** `CommandPalette/CommandPalette.tsx` (KBarProvider + KBarPortal/Positioner/Animator/Search + result renderer + `PaletteShortcutBindings` for the `g h`/`g p`/`g t` chords + cheatsheet action + `MobileFab`), `actions.ts` (`buildNavigateActions` auto-populating from `listAdminPanes()`), `shortcuts.ts` (the catalogue — ⌘K/⌘S/⌘↵/⌘⇧P/`?`/`/`/chords), `Cheatsheet.tsx` (AntD modal auto-rendered off `shortcuts.ts`), `CommandPalette.scss`. `AdminApp.tsx` mounted the provider — but only around the *build* view, so the palette was dead on every other pane and the top-bar trigger (which still opened a separate **legacy** AntD-modal `CommandPalette.tsx`) had no provider in scope. **What this jump added:** (1) `buildUtilityActions()` — `Preview site` / `Open blog` actions beyond pure pane-navigation; (2) `CommandPaletteTrigger` — a `useKBar`-backed top-bar button; (3) relocated the `KBarProvider` mount from `AdminApp.tsx` up to `UserStatusBar.tsx` so it wraps the top-bar trigger **and** every dispatched pane — ⌘K now works from any admin route (acceptance #1); (4) wired the `AdminTopBar` Command button to `CommandPaletteTrigger` (replacing the legacy modal trigger); (5) imported `CommandPalette.scss` into the component + added `.admin-topbar-command-trigger` styling; (6) **deleted** the legacy `ui/admin/shell/CommandPalette.tsx` AntD-modal palette + its `useCommandPaletteHotkey` hook (kbar binds `$mod+k` itself). **Destinations are sourced** entirely from `adminUILoaderRegistry.listAdminPanes()` — the same registry the shell dispatches panes off — so a new `*AdminUILoader` with an `adminPane` descriptor appears in the palette with zero hand-maintained list. `data-testid`: `cmdk-search` (input), `cmdk-result-{id}` / `cmdk-section-{label}` (results), `cmdk-trigger` (top-bar button), `cmdk-mobile-fab`, `cmdk-cheatsheet-*`. i18n keys already present in `en.json` + `lv.json` (admin i18n has no `ru` locale — the spec's `ru.json` mention is stale). **Deferred:** per-feature `useRegisterActions` calls (⌘S save / ⌘↵ publish / ⌘⇧P preview / `/` list-search bind from inside each editor / list view) — the `shortcuts.ts` catalogue + cheatsheet already list them and `PaletteShortcutBindings` is the registration site, but the per-pane closures land with each feature pane (the spec explicitly allows this as a fast follow-up, "Per-feature `useRegisterActions` calls can land in the same chunk if light, or as a fast follow-up per pane"); e2e smoke spec. Spec doc [admin/admin-command-palette.md](admin/admin-command-palette.md) marked shipped. |

## 2026-05-14 — W6a receipt + transactional emails (9-template surface)

| # | Notes |
|---|---|
| **EML receipt emails** | The full 9-template transactional surface from [storefront/storefront-receipt-emails.md](storefront/storefront-receipt-emails.md). Most of the infrastructure (`_shared/{layout,components,theme}.ts` shell + table-based progress stepper + bulletproof button + `EmailLogService` audit + `email.preview` MCP tool + admin preview pane) already shipped earlier under W6a; this jump closed the template gap. **Already existed + registered:** `receipt` (≡ spec `orderConfirmation`, visual timeline), `order-confirmation` (subject-only alias), `shipped`, `magic-link`, `password-reset`, `account-welcome`, `abandoned-cart`. **Existed but were never registered — now registered:** `verify-email`, `car-reservation-confirmation` (visual timeline: Reserved → Verification → Pickup scheduled → Handover). **Net-new this jump:** `inquiry-acknowledgement` (visitor-facing contact-form receipt, one CTA + 24h response promise), `scheduled-publish-failed` (operator-targeted, release facts + error + retry CTA, no unsubscribe footer), `low-stock-alert` (operator-targeted, compact reorder-point worklist table), `saved-search-alert` (customer-facing, image-left result-preview list + filter-URL CTA). All 13 templates now in `registry.ts`; `email.preview` MCP tool description updated to list them. **Live triggers wired:** `inquiry-acknowledgement` → `ui/client/pages/api/inquiry.ts` (best-effort send to the submitter after the operator notification; failure never affects the API response). `receipt` was already wired to the order-paid mailer closure in `OrdersServiceLoader.ts`; `magic-link` to `pages/api/auth/magic-request.ts`. **Triggers pending (template ships ahead of trigger):** `scheduled-publish-failed` — `ReleaseService` has no `scheduledFor` worker and no mail-config/recipient access (the service only takes `db` + `client`); needs the scheduler worker + operator-recipient resolution. `low-stock-alert` — `InventoryService` tracks per-row `stock` but has no reorder-point / threshold concept. `saved-search-alert` — no saved-search backend exists yet (only the `ui/client/modules/SavedSearchList` type stub); lands with W6b faceted-filter saved searches. Spec doc amended with a Status section. |

## 2026-05-14 — Admin dark-mode audit (global-first token set)

| # | Notes |
|---|---|
| **DM-audit** | Admin dark mode rebuilt global-first through the AntD `ConfigProvider` instead of per-component CSS overrides. Mechanism: `ui/admin/lib/adminTheme.ts` (`buildAdminTheme(dark)`) ships a tuned light + dark token set — `colorBg{Layout,Container,Elevated}` three-elevation ramp, lifted `colorText{,Secondary,Tertiary,Quaternary}` ramp for WCAG-AA contrast on dark chrome, visible `colorBorder`, darker `colorBgMask`, pinned `colorLink`/`colorLinkHover` — plus thin token-referencing `components.{Layout,Table,Tabs,Modal,Drawer,Menu}` overrides (no hardcoded hex). `cssVar: {key: 'admin'}` is enabled, exposing every token as a `--ant-color-*` custom property. Persistence + toggle: `ui/admin/lib/adminDarkMode.ts` is a module-scoped store (mirrors `adminMode.ts`) — localStorage `admin.darkMode` (`'1'`/`'0'`), stamps `data-admin-theme` on `<html>`, cross-tab `storage` sync, `useAdminDarkMode()` hook + new `subscribeAdminDarkMode()` plain subscription for the `AdminApp` class component. `ui/admin/shell/DarkModeSwitcher.tsx` is the top-bar `Switch` (`data-testid="admin-dark-mode-switch"`, `data-state`), already wired into `AdminTopBar` desktop + mobile-drawer. Fixed `AdminApp.tsx`: it imported the new libs but still ran dead local `darkMode` state + a broken inline `ConfigProvider` referencing an undefined `antdTheme` — now subscribes to the store and renders `buildAdminTheme(this.state.darkMode)`. SCSS audit: `AppLoginWrapper.scss` hardcoded `#fff`/`rgba(0,0,0,…)`/`#555` (top bar didn't flip) → now consumes `--admin-*` tokens; redundant `[data-admin-theme="dark"] .app-login-wrapper` override block in `AdminDarkMode.scss` removed (one palette, no parallel system). **Left for a manual visual pass:** the 5 representative panes (Page editor, Posts, Products, Themes, System/Diagnostics) — Stitch screenshot review wasn't runnable; the global token set is the first pass, per-feature stragglers in `ui/admin/features/*` still need a manual check. Spec doc [admin/admin-dark-mode-audit.md](admin/admin-dark-mode-audit.md) amended. |

## 2026-05-14 — F6 site-mode toggle (scroll vs multipage)

| # | Notes |
|---|---|
| **F6 site-mode toggle** | Per-site render-mode switch shipped as `siteFlags.layoutMode: 'tabs' \| 'scroll' \| 'auto'` (the spec's working name `siteMode` / `'multipage'` was superseded — `'tabs'` ≡ spec `'multipage'`; the codebase already carried `layoutMode` with identical semantics, so the implementation extended it rather than introducing a duplicate flag). Full chunk, one deliverable: flag definition + default (`'tabs'`) + read path (`SiteFlagsService.get/save` + `resolveLayoutMode()` resolving `'auto'`→`'tabs'`), public-site render branch (`ui/client/pages/app.tsx` — scroll mode stacks every page as `<section id>`, tabs mode keeps F1 per-page routing; `[...slug].tsx` hard-redirects deep paths to `/#anchor` in scroll mode), nav/footer mode-aware rendering (`MainMenu`, `SiteFooter` rewrites page URLs to `#anchor`, `ScrollNav` with IntersectionObserver smooth-scroll), anchor registry (`services/features/Anchors/AnchorRegistry.ts` — `siteMode`-aware href shapes), admin control (`ui/admin/features/Navigation/Layout.tsx` three-card Radio.Group + `LayoutViewModel.setMode`, `data-testid` on every card/radio), MCP coverage (`site.setLayoutMode` tool — dedicated tool rather than overloading `site.setFeatureFlag`, which targets boot-time feature manifests; `anchors.*` tools accept `siteMode` for href-shape parity), runbook ([docs/runbooks/site-mode-switch.md](../runbooks/site-mode-switch.md)), architecture doc ([docs/architecture/site-flags.md](../architecture/site-flags.md)). All four spec "Open questions" resolved as recommended: default `auto`→`tabs` (no page-count/footer heuristic), sub-pages flatten to top-level sections in scroll mode. Spec doc [platform/site-mode-toggle.md](platform/site-mode-toggle.md) marked shipped + amended with the naming reconciliation. |

## 2026-05-08 — Terraform/Kamal funisimo migration + admin polish + image hygiene

| # | Notes |
|---|---|
| **Terraform/Kamal funisimo** | Replaced 250-line inline ssh deploy with `kamal app boot --version=<sha>` + docker network alias rotation. Caddy stays the public front (kamal-proxy disabled via role-level `proxy: false` — operator decision: option A). CI flow: `test-and-typecheck` → `ghcr-push` → `deploy-funisimo`, ~10-15 min master-push to live. `terraform/environments/funisimo/` imports the existing droplet + reserved IP cleanly (`No changes` plan). Skyclimber stays on legacy ssh+rsync until its own DO API token arrives. 5 spec divergences + 9 footguns documented in [terraform-kamal-migration.md](platform/terraform-kamal-migration.md); operator-truth runbook at [docs/runbooks/kamal-deploy.md](../runbooks/kamal-deploy.md); local validation flow at [docs/runbooks/local-prod-test.md](../runbooks/local-prod-test.md). |
| **GHCR retention** | New weekly job at `.github/workflows/ghcr-retention.yml` keeps last 8 versions of `ghcr.io/gatispriede/cms` (each push = 1 tagged manifest + 2 untagged attestation companions = 3 entries, so 8 ≈ ~2-3 master deploys of headroom). Manual one-shot pruned 22 → 6 versions during the migration day. |
| **Standalone image refactor — ATTEMPTED + REVERTED** | Tried Next.js `output: 'standalone'` to shrink runtime image 2.03 GB → 507 MB. Two issues forced a revert same day: (a) tracer skipped ESM-conditional `dist/` files (`@reduxjs/toolkit/dist/redux-toolkit.modern.mjs` missing → `/admin` 500), (b) static `getStaticProps` for `/` baked empty-Mongo state from `tools/docker-prebuild.js` so homepage served the marketing landing for ~1h after every deploy. Revert kept the ~2 GB multi-stage image with all routes working. Re-attempt deferred until the local-prod-test workflow gates the change. |
| **homepage `getServerSideProps`** | Even with the standalone revert, `/`'s `getStaticProps + revalidate: 3600` still baked an empty-Mongo `showLanding=true` at image build time. Switched to `getServerSideProps` so `showLanding` evaluates against runtime Mongo per request. No staleness window after deploy; SWR cache layer covers repeat-visitor performance. Same fix pattern applies if other pages develop the same bug. |
| **user-create `grants` strip** | `UsersViewModel.save()` sent `grants: [...]` on the addUser/updateUser payloads but `InUser` GraphQL input type doesn't carry that field — admin "Add user" returned GraphQL 400. Added `UserApi.toMutationPayload()` which omits `grants` (persisted via separate Permissions API) before send. |
| **admin desktop AreaNav** | Operator screenshot 2026-05-08 caught a regression — AreaNav rail rendered full-width across the top instead of left-side. Cause: I added a mobile @media block but never wrote a default desktop layout for `.admin-area-layout`. Explicit `display: flex; flex-direction: row` on the wrapper for the default state; mobile block's `column !important` still wins below 768 px. |
| **CI hygiene** | E2E + visual regression workflows removed from the auto-trigger pipeline (operator: "we run those locally"). `e2e.yml` deleted; `visual:` job stripped from `ci.yml`. Skyclimber legacy deploy `auto: false` (operator paused). Funisimo legacy deploy `auto: false` (Kamal owns it). |
| **droplet hygiene** | funisimo: 39 GB used → 11 GB used (`docker system prune -af --volumes` + `journalctl --vacuum-time=7d`, freed ~27.5 GB of accumulated build cache + orphan mongo volumes from months of legacy compose deploys). Skyclimber: 60 GB used → 20 GB used (~43 GB reclaimed). Both at 23-26% disk after cleanup. |
| **secrets.md updated** | Added Terraform Cloud token, DO PAT location, GHCR namespace, GHCR_PAT, Kamal runtime secret list. SSH user note corrected to `root` (the `gatis` operator user described earlier was aspirational, not deployed). |

## 2026-05-04 — F7 slug source-of-truth + F8 MCP coverage + email-config + UX polish

| # | Notes |
|---|---|
| **F7** | Slug single source of truth — canonical `normalizeSlugForMatch` in `shared/utils/slug.ts`, re-exported by `services/features/Navigation/NavigationService.ts` and `ui/client/lib/slugChain.ts`. `pageId` threaded `[...slug].tsx` → `app.tsx`; `findIdForActiveTab` rewritten to exact `tab.id === pageId` lookup (no string matching). Three `menuPages` projections in `app.tsx` rekeyed off `p.id` — fixes the bug where a sub-page rendered as a flat sibling instead of under its root. Two commits: `0d9de06` (canonical helper + initial sweep), `f510beb` (third desktop-MainMenu projection missed by sweep). 17 new tests; 790 total green. See [slug-source-of-truth.md](platform/slug-source-of-truth.md). |
| **F8** | MCP coverage to real-world-ready — 38 → 87 tools, drift CI 0/0. `compose(...)` wrappers for idempotency, audit, rate-limit, error envelope, validation. `defineTool({...meta}, handler)` registration pattern. Four commits: `1ac5d0d` (W1), `073a59e` (W1 phase 2 — sweep all 45 tools onto compose), `152e2c0` (W2 — P0+P1, registry 66/70), `e300c9a` (W3 — P2 + runbook + E2E spec, registry 87/70). Roadmap snapshot at `bf957e9`. See [mcp-real-world-ready.md](platform/mcp-real-world-ready.md). Streaming transport + plugin SDK + E2E un-skip deferred to post-merge. |
| **email-config** | SMTP / Resend / Disabled providers behind a `mailConfig` site-flag, AES-GCM encryption via `SECRETBOX_KEY`. Admin pane at System → Email with provider switcher, test-send button, per-provider validation, specific missing-fields error surfacing. Four commits: `1c9cd8a` (foundation — SecretBox + EmailService + SiteFlags shape), `ea09fcd` (admin UI + MCP tools + Resend), `1fb8ebb` (specific missing-fields error), `5cf5cd8` (sanitizeMailConfig encrypts plaintext on save — admin form sent `resendApiKey` plaintext, server was dropping it). |
| **admin click-parent edits (option B)** | Admin sider parent-row title click now navigates/edits like any leaf; a chevron Button in the label toggles expand/collapse via a controlled `openKeys[]` state. Tree is flattened manually (no AntD `items[].children` so SubMenu can't intercept clicks). Default-expand all parents on first load; subsequent reloads preserve operator's choice. `bad78a9`. |
| **themed error pages** | `404.tsx` (replaces "Not found" div), new `500.tsx`, new `_error.tsx` catch-all — all share `<ErrorScreen/>` (`ui/client/features/ErrorScreen/`). Pulls `--background` / `--ink` / `--accent` / `--font-display` / `--theme-borderRadius` from active theme; leads with `<Logo/>` so the brand mark mirrors the favicon role. `8711f37`. |
| **admin nav reorg** | System area-nav reordered (users → email → inquiries → flags → rest below); SEO becomes its own area (Analytics moves there); Errors → System; Bundle first in Release. Two commits: `1c4dece` + `10d0f03`. |
| **mcp-call helper** | `tools/scripts/mcp-call.mjs` — drives any MCP tool against the local dev DB through actual `McpServer.dispatch`. Used to apply skyclimber bundle patches via real `module.add` instead of editing JSON. `c78171e`. |

## 2026-05-03 — F1 sub-pages + F2 data integrity blitz

| # | Notes |
|---|---|
| **F1** | Sub-pages — full surface. Data model (`parent?`, `slug?` on IPage/INavigation; `slug` widened to `string \| Record<LocaleCode, string>` via JSON scalar with non-breaking SDL), `setParent` mutation with server cycle + 3-level depth cap + client cycle guard, slug backfill, `findPageBySlugChain` resolver (locale-aware, default-locale fallback, slugified-page last-resort), parent-scoped slug uniqueness, `[...slug].tsx` catch-all with full slug-chain prop, locale-aware sitemap with `<xhtml:link rel="alternate" hreflang>`, AntD `<Menu mode="horizontal">` + `SubMenu` with 4-theme SCSS, anchor registry walks parent chain (`Services → Cleaning`), admin sider nested rendering, Parent Select, Breadcrumb, delete-with-children prompt, mobile drawer nested rendering with chevron-rotate SCSS polish, SSR theme-name on `<body>`, decorative outer `<Tabs>` retired, gqty regen, `parent` round-trips through `InNavigation`, admin per-locale slug editor with bare-string→Record promote. ~118 new tests. |
| **F2 idempotency** | Engine — Redis primary + Mongo fallback, in-flight collapse, 5-min TTL via `IDEMPOTENCY_TTL_SECONDS`. Boot-wired in `setupClient`. `idempotencyKey: String` arg on 7 destructive mutations (deletePost, deleteNavigationItem, removeSectionItem, deleteLanguage, deleteTheme, removeUser, revokePermission). `useGuardedAction` React hook + `GuardedAction` VM class with `onPendingChange`, wired into Posts/Themes/Users/MCP/AdminApp delete flows. 11 + 6 unit tests. |
| **F2 cascade** | Engine + `cascadeRules` on `FeatureManifest` with `kind` discriminator (`collection-move` default + `doc-mutate` for singleton fields). Soft-delete to `*.trash` collections with 24h Mongo TTL. `cascadeRestore(trashGroup)` mutation (no-op for doc-mutate rules). Admin Trash pane at `/admin/release/trash`. Wired into `removeSectionItem`/`deleteNavigationItem`/`deletePost`. **Cascade rules:** Permissions (page-scoped grants follow page deletions), Seo (doc-mutate `$unset SiteSeo.pages.<slug>`), Posts (page-pinned posts via `pageId?`), Navigation→Navigation recursive (sub-page tree, depth-bounded with visited-set). Transaction with single-node best-effort fallback. 12+ cascade tests. |
| **F2 Posts pinning** | `pageId?: string` on `IPost`, cascade rule (Navigation→Posts via `matchByParentId: id => ({pageId: id})`), admin "Pin to page" Select with allowClear + search, page-list fetched on `PostsViewModel.refresh()`. Public URL stays `/blog/${slug}` regardless of pin. 8 new tests. |
| **gqty cold-load sweep** | Same anti-pattern as ThemeApi.listThemes applied to 5 APIs / 6 methods: `SiteFlagsApi.get`, `FooterApi.get`, `SiteSeoApi.get`, `TranslationMetaApi.get`, `AuditApi.listCollections`/`listActors`. Each carries the same 4-line comment so `git grep "Direct-route bug"` enumerates every site that migrated. |
| **E2E coverage for new features** | 5 spec files (`tests/e2e/features/{sub-pages,trash,idempotency,aui-simplified}.spec.ts`, `tests/e2e/modules/timeline-anchors.spec.ts`) — 16 tests + 9 `test.skip`s pending data-testids. Aggregated TODO testid list captured for future sweep. |
| **E-commerce testid sweep + seed fixtures** | `data-testid` wired across Products/Orders admin panes + storefront product card + product detail + cart/checkout flow + order confirmation. New `tests/e2e/fixtures/seedFactories.ts` with `seedProduct` / `seedOrder` / `createCleanupRegistry` (direct-Mongo writes, per-test cleanup registry). 1 product-create flow un-skipped; the rest still need in-test seed wiring. Two UI-design gaps flagged: Inventory pane lacks per-product editor (sync-status only); Orders detail uses Drawer not URL nav. |
| **Mobile drawer SCSS polish** | `+74` lines in `MobileNav.scss`: `.mobile-nav-row` flex container, `.mobile-nav-toggle` chevron with 38px hit target, `.mobile-nav-row.is-expanded .mobile-nav-toggle > span` 180deg rotate transition (mirrors existing caret pattern), `.mobile-nav-children` subtle bg, `.mobile-nav-item.is-child` 42px indent. All theme tokens reused; theme-agnostic via existing CSS vars (Paper/Studio/Industrial/HighContrast). |
| **EL-feat-rest** | `resourceGated` for Navigation (7 muts: `{feature, page}` for slug-bearing, `{feature}` for cross-page), Seo (`saveSiteFlags`/`saveSiteSeo`), Permissions (`grantPermission`/`revokePermission`), Users (`addUser`/`updateUser`/`removeUser`; `setMyAdminUiMode` deliberately ungated). 7 gated tests. **30 admin mutations gated** total across the codebase. |
| **API integration tests** | `setup.integration.test.ts` (idempotent admin seed), `export-import.integration.test.ts` (round-trip + 405 + 400 malformed), `rescan-images.integration.test.ts` (orphan discovery + idempotent). 9 tests via mongodb-memory-server with mocked auth/origin/rate-limit. |
| **C13b** | Timeline per-entry stable anchors — `#${slugifyAnchor(company-role)}` on `<h3 className="timeline__who">`. `anchorRegistry.ts` walks `entries[]` so the picker surfaces them under "Timeline entries". Manifesto stays section-only (single body paragraph, no row model). |
| **AUI simplified panes** | `ThemeSimplifiedView.tsx` — preset gallery only, no token editor. `PostsSimplifiedView.tsx` — title + body + cover, every save publishes (no draft state, no tags/author/excerpt/slug/blog-visible toggle). Both wired through `*AdminUILoader.modes.simplified`. Bulk-migrations queue emptied. |
| **Bundle import sanitizer** | `services/features/Bundle/BundleService.ts` — accepts spaces, parens, unicode (collapsed to `_`); rejects only on null byte, control char, `..`, disallowed extension. Camera/screenshot filenames like `IMG_0001 (1).jpg` and `Screenshot 2026-04-26 162952.png` round-trip cleanly now. |
| **Top-bar split** | Admin top bar — nav actions (Build / Client config / Content / SEO / Release / System / Preview / Command) on the left; account + chrome (Language / Mode / Theme / Sign out) on the right via `.app-login-wrapper__spacer`. |
| **Themes gqty bug** | `ThemeApi.listThemes` switched from `gqty.resolve` to raw `fetch('/api/graphql')` POST — fixes empty-payload on cold direct route to `/admin/client-config/themes`. Same anti-pattern flagged for SiteFlags/Footer/SiteSeo/TranslationMeta/Audit APIs (E2E backlog item 3). |
| **E-commerce E2E specs** | 5 mount-level specs under `tests/e2e/ecommerce/` (products / cart / checkout / inventory / orders). Deeper flows guarded by `test.skip` pending data-testids + `seedProduct`/`seedOrder` fixtures (see TODO at top of each spec). |
| **F3 cancelled** | F3 `/v1/**` namespace migration cancelled 2026-05-03 — see [v1-url-namespace.md](platform/v1-url-namespace.md) postmortem. Pages Router special-cases `pages/api/*`; moving it broke server/browser bundle isolation. Without API freedom, moving `/admin/*` alone wasn't worth the cost. Constraint recorded in [PROJECT_ANALYSIS.md](../PROJECT_ANALYSIS.md): customer pages cannot use slugs `admin` or `api`. |
| **Cleanup closeouts** | Phase 3 admin-segregation deletion → demoted to runbook (gated on observation window). C13b shipped. C17 closed deferred-on-demand. AUI-mode reframed as per-pane opt-in. tests-remaining.md retired. |
| **Products test fix** | `ProductsServiceLoader.test.ts` deep-equal on `authz` was comparing arrow-function identity; replaced with sub-field assertions + key-set check. 644/644 → 651/652 (in-flight cascade recursion test expected to clear). |

## Production / ops

| # | Date | Notes |
|---|------|-------|
| P1 | 2026-04 | First-boot admin password — see [production/first-boot-admin-password.md](production/first-boot-admin-password.md) |
| P2 | 2026-05-03 | Automatic deployment — `.github/workflows/deploy.yml`, gated on `vars.DEPLOY_ENABLED`; runbook [docs/runbooks/automatic-deployment.md](../runbooks/automatic-deployment.md) |
| P3 | 2026-05-03 | DigitalOcean domain + TLS — Caddyfile env-driven (`DOMAIN`, `SITE_DOMAIN_WWW`), auto-HTTPS; runbook [docs/runbooks/digitalocean-domain.md](../runbooks/digitalocean-domain.md) |
| P4 | 2026-05-03 | Seamless (blue/green) deployment — `app-blue` / `app-green` (Compose `seamless` profile), Caddy `{$ACTIVE_UPSTREAM}` switch, orchestrator `tools/blue-green-deploy.sh`; gated by `vars.SEAMLESS_DEPLOY`; runbook [docs/runbooks/seamless-deployment.md](../runbooks/seamless-deployment.md) |
| P5 | 2026-05-03 | MongoDB auth — `tools/mongo-bootstrap.sh` + `mongoConfig.buildMongoUri`; runbook [docs/runbooks/mongo-auth-setup.md](../runbooks/mongo-auth-setup.md) (dev path preserved) |

## Content editor

| # | Date | Notes |
|---|------|-------|
| N15 | 2026-04-24 | Folder reshape — `ui/{client,admin}/` + `services/` + `shared/` + `tools/` + `infra/`. See [migration-mapping.md](_meta/migration-mapping.md) |
| C1 | 2026-04-24 | Themes as files — four editorial presets in `ui/client/themes/*.json`, seeded on boot when missing; admin "Reset to preset" overwrites DB row from disk |
| C2 | 2026-04-24 | Image optimization on upload (partial) — shared `imageOptimize.ts` pipeline (resize 1920 cap + recompress + strip EXIF + size guard) on both single + batch uploads |
| C3 | 2026-04-24 | Bulk image upload with ratio — `/api/upload-batch` (sharp cover-crop + EXIF strip + collision-safe), `BulkImageUploadModal`, GalleryEditor "Bulk upload" button |
| C4 | 2026-04-24 | Drag-drop images modules — extended `useImageDrop` with OS file upload + URL re-host + per-file AntD toasts; shared `<ImageDropTarget>` wrapper + SCSS |
| C5 | 2026-04-24 | Picker improvements — sort dropdown (recent/name/size), two-column grid + sticky resizable preview panel, always-visible name+size caption, per-tile info drawer, keyboard navigation |
| C6 | 2026-04-24 | Gallery improvements (partial) — aspect-ratio lock, Masonry style, per-tile `href`, up/down reorder; custom lightbox + drag-reorder deferred |
| C7 | 2026-04-24 | Logo style options — site-wide Logo `style` field + `.logo--{default,bordered,framed,circle}` SCSS + admin Style select |
| C8 | 2026-04-24 | Module transparency style — `transparent` flag on `ISection` + `.is-transparent` renderer rule + admin Switch with contrast hint |
| C9 | 2026-05-02 | Production caching — `bootId` + per-feature version stamps in Redis (`cacheVersion.ts`), `X-Cms-Cache-Tag` header, Caddy SWR fragment + [runbook](../runbooks/caddy-cache.md), DataLoader fold-in via `ServiceLoader.batchAccessors` (Posts is the demo) |
| C10 | 2026-04-24 | Admin modules preview page — `/admin/modules-preview` with sample fixture per `EItemType`, theme-switcher dropdown, transparent-bg toggle, module-name filter; sample-coverage test |
| C11 | 2026-04-24 | Admin menu icons — icons on every main-nav button + settings tab + sidebar row; adapter extended with 11 new lucide mappings |
| C12 | 2026-05-02 | Image width/height respect — `<SizedImage>` helper + `data-sized` opt-out on parent `.image` wrappers; SCSS carve-outs in Logo / Gallery / Carousel |
| C13 | 2026-05-02 | Link target autosearch — picker UX + readable section labels + stable-anchor emission for Hero / BlogFeed / ProjectCard; `RevealOnScroll` forwards `id`. Manifesto / Timeline anchor support deferred (no single representative title) |
| C14 | 2026-05-02 | Inline-edit text after add — picker → primary-text-input autofocus on add (no autofocus on edit) |
| C15 | already shipped | Admin dark-mode theme — top-bar Switch (`AdminApp.tsx`), AntD `darkAlgorithm` via `ConfigProvider`, localStorage-persisted (`admin.darkMode`). Hoisted to top-top bar 2026-05-03 with chrome-only SCSS scoping |
| C16 | already shipped | Admin/client side-by-side editor layout — `AddNewSectionItem.tsx` Drawer renders `[editor tabs] | [live preview]` 2-column grid for every module type |
| C18 | 2026-05-02 | Image-ref + link-ref schema convergence — `IImageRef` + `ILinkRef` shared shapes; `<ImageRefInput>` + `<LinkRefInput>` shared editors; per-module ContentManager normalises legacy → new shape |
| BUG | 2026-04-24 | Client report — Hero #1 + #2: `clamp()` font floors + `overflow-wrap: anywhere` + theme-tunable `--hero-scrim-opacity` scrim. Services #3 not reproducible, locked with regression tests |

## Build / DX

| # | Date | Notes |
|---|------|-------|
| Q1+VM4 | 2026-05-03 | ESLint flat config + VM4 lint rule banning `useState` in `ui/admin/features/**` |
| Q2 | 2026-05-03 | VM3 — Themes + Translations panes migrated (commit b79d221) |
| **VM3-rest** | **2026-05-03 (commit d942c50)** | VM3 closeout — final 11 panes migrated off `useState`: Agent, Analytics, Bundle×3, ModulePicker, AddNewLanguageDialog, ImageRail, FeatureFlags, RestartRequiredBanner, SEO, FontPicker. **17/17 — zero `eslint-disable-next-line no-restricted-imports` markers remain** in `ui/admin/` |
| **L4-bulk** | **2026-05-03 (commit 4b56c27)** | `ClientUILoader.publicRoutes` + `gatePath()` helper auto-applies `withFeatureGate`; 4 features migrated (`products`, `posts`, `cart`, `orders`); per-page inline gates dropped from 4 page files. Plus item-types registry composed from per-feature halves: `CLIENT_ITEM_TYPES` (Display) + `ADMIN_ITEM_TYPE_EDITORS` (Editor + metadata); 24 entries split, public API unchanged |
| Q4 | 2026-05-03 | Visual regression baselines — Playwright `toHaveScreenshot()` config + 58 spec entries (48 module Display+Editor, 9 critical surfaces, 1 Footer); CI shard config; runbook [docs/runbooks/visual-regressions.md](../runbooks/visual-regressions.md). **Image capture pending** — see open backlog |
| Q5 | 2026-05-03 | Admin-segregation Phase 3 observability — Next edge middleware logs every legacy-route hit via `/api/log/error` (level: warn, scope: legacy-route) before redirect fires. **Deletion pending** — see open backlog |
| Q6 | 2026-05-03 | Conditional gqty regen on prebuild — `tools/check-schema-stale.js` mtime-checks `services/api/schema.graphql` vs generated client |
| **mcpAgentTools-fix** | **2026-05-03 (commit 583cfc2)** | Long-standing `services/agent/mcpAgentTools.ts` typecheck error resolved — `mcpToToolDef` narrows `inputSchema: unknown` at the call site with empty fallback |
| **AppDockerfile-sha** | **2026-05-03 (commit 583cfc2)** | `ARG GIT_SHA=unknown` writes `/app/.git-sha` inside the image; `tools/blue-green-deploy.sh` passes `--build-arg GIT_SHA="$TARGET_SHA"`. Resolves the no-op SHA-verification flagged in P4 |

## Auth / multi-tenancy

| # | Date | Notes |
|---|------|-------|
| Q10 | 2026-05-03 | Edit-levels three-dimension grants (FeatureGrant / PageGrant / LocaleGrant) — intersection semantics, admin-role bypass, Posts wired as the reference. See [docs/architecture/auth-roles.md](../architecture/auth-roles.md) |
| **EL-feat** | **2026-05-03 (commit 3248e86)** | Per-feature `resourceGated` extended beyond Posts: Products, Inventory, Orders (admin-mutations only), Footer, Themes, Languages (`{feature, locale}` for translator scoping), Bundle (declared, HTTP routes still). **18 admin GraphQL mutations now grant-gated.** 8 `<Loader>.gated.test.ts` files added |
| **EL-i18n** | **2026-05-03 (commit 3248e86)** | `runI18nGrantMigration(db)` boot-once script wired into `LanguagesServiceLoader.onBoot`. Idempotent: when `siteFlags.inlineTranslationEdit === true`, grants `translator` to every editor-rank user, then sets the flag false. 5 tests covering no-op + on-flip + idempotency |
| **AUI-mcp** | **2026-05-03 (commit 4b56c27)** | MCP execution gate — `enforceModeForTool(userId, toolId)` + `FeatureRestrictedError` + `ADVANCED_TOOLS` allowlist. **8 MCP tools gated as advanced-only**: `site.{revalidate,regenerateSchema,setFeatureFlag,clearFeatureFlag}`, `auth.resetLockouts`, `inventory.syncDelta`, `theme.{setActive,update}`, `section.delete`. 8 passing tests |
| **CA-geo** | **2026-05-03 (commit 1bc3fce)** | Client-analytics country lookup — embedded IPv4→country dataset (CC0, IP2Location LITE DB1) at `infra/datasets/ip-to-country.json`; `geoLookup()` does synchronous binary search at ingest; **IP discarded** before Mongo write; admin Analytics gains top-N country table; runbook [docs/runbooks/analytics-geolookup.md](../runbooks/analytics-geolookup.md). 17 tests |
| **AUI-todo** | **2026-05-03 (commit 4b56c27)** | Things-to-do panel mounts at `/admin` (both modes) — 4 collectors via `Promise.allSettled` for graceful degrade: `draftPosts`, `unpublishedChanges`, `inventoryDeadLetters`, `pendingOrders`. Translations + low-stock noted as next iteration |
| **grants-UI** | **2026-05-03 (commit 69482ba)** | Grants assignment in Users pane — three free-text `Select mode="tags"` swapped for constrained `mode="multiple"` with options pulled live from `getFeatureFlags` / `getNavigationCollection` / `getLanguages`. Per-source try/catch; `showSearch` + `optionFilterProp="label"`. Coding principle saved to `memory/feedback_predefined_selections.md` |

## Go-to-market (Q7)

| # | Date | Notes |
|---|------|-------|
| Q7-wizard | 2026-05-03 | First-run onboarding wizard — site name + admin account + theme pick at `/admin/onboarding`. Server-side fresh-install gate via `/api/onboarding/is-fresh-install` |
| Q7-landing | 2026-05-03 | Marketing landing page — `/welcome` always-on; `/` flips to landing on fresh installs (`pages.length === 0`). Visual baseline added |
| Q7-docs | 2026-05-03 | Docs site — `docs/site/*.md` source; `tools/seed-docs-bundle.js` generates a CMS bundle; admin imports it; `/docs` index + `/docs/[slug]` per-page routes |

## Misc

| # | Date | Notes |
|---|------|-------|
| favicon | 2026-05-03 | Dynamic `/api/favicon` route serves the active site's Logo SVG when present, falls back to a monogram SVG built from siteName initial(s) |
| simplified-mode-cuts | 2026-05-03 | Admin UI mode toggle now visibly changes the UI: top-bar areas (SEO/Release/System), content area-nav (Products/Inventory/Orders), Layout pane sub-sections, module picker (8 of 24 types), Action tab, Style sub-options, Publish button, click-to-edit on module body. See [docs/features/platform/admin-ui-modes.md](../features/platform/admin-ui-modes.md) |
| dark-mode-hoist | 2026-05-03 | Dark/light + simplified/advanced switchers hoisted from `AdminApp` secondary header to `UserStatusBar` top-top bar — visible on every admin route. Chrome-only SCSS scoping so the build-page preview stays in client theme |
| components-drop | 2026-05-03 | `ui/client/components/` removed; Marketing + Cart moved to `ui/client/features/{Marketing,Cart}/` to match target architecture |
