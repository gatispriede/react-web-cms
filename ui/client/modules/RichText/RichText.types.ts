export interface IRichText {
    value: string;
}

export enum ERichTextStyle {
    Default = "default",
    CenteredBoxed = "centeredBoxed",
    Magazine = "magazine",
    Mono = "mono",
    Letter = "letter"
}
