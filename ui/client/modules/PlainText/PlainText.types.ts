export interface IPlainTextContent {
    value: string;
}

export enum EPlainTextStyle {
    Default = "default",
    Centered = "centered",
    CenteredBoxed = "centeredBoxed",
    Lede = "lede",
    Quote = "quote",
    DropCap = "dropCap"
}
